 
listView('adkasdjasidjaiosdj Jobs') {
    description('adkasdjasidjaiosdj Jobs')
    jobs {
        regex('adkasdjasidjaiosdj_.+')
    }
    columns {
        status()
        weather()
        name()
        lastSuccess()
        lastFailure()
        lastDuration()
        buildButton()
    }
}
